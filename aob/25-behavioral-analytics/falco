alco has been installed on Node controlplane and it runs as a service.

It's configured to log to syslog, and this is where the verification for this scenario also looks.

Cause the rule "shell in a container" to log by:

creating a new Pod image nginx:alpine
kubectl exec pod -- sh into it
check the Falco logs contain a related output

Tip

service falco status

cat /var/log/syslog | grep falco

Solution

k run pod --image=nginx:alpine

k exec -it pod -- sh

cat /var/log/syslog | grep falco | grep shell

=====
Mar 14 21:22:17 controlplane falco[26653]: 21:22:17.568640395: Notice A shell was spawned in a container with an attached terminal (user=kc-internal user_loginuid=-1 nginx (id=217fe38f007f) shell=sh parent=runc cmdline=sh terminal=34816 container_id=217fe38f007f image=docker.io/library/nginx)
Mar 14 21:22:17 controlplane falco[26653]: 21:22:17.568640395: Notice A shell was spawned in a container with an attached terminal (user=kc-internal user_loginuid=-1 nginx (id=217fe38f007f) shell=sh parent=runc cmdline=sh terminal=34816 container_id=217fe38f007f image=docker.io/library/nginx)

=====

Falco Change Rule
Change a Falco Rule
Change the Falco rule "shell in a container" to:

also include user_id=%user.uid
also include repo=%container.image.repository
start the log lines with NEW SHELL!!!
Cause syslog output again by creating a shell in that Pod.

Verify the syslogs contain the new data.


Tip
https://falco.org/docs/rules/supported-fields


cd /etc/falco/

grep -ri "shell in"

Solution

cd /etc/falco/

cp falco_rules.yaml falco_rules.local.yaml

vim falco_rules.local.yaml

- rule: Terminal shell in container
  desc: A shell was used as the entrypoint/exec point into a container with an attached terminal.
  condition: >
    spawned_process and container
    and shell_procs and proc.tty != 0
    and container_entrypoint
    and not user_expected_terminal_shell_in_container_conditions
  output: >
    NEW SHELL!!! (user_id=%user.uid repo=%container.image.repository %user.uiduser=%user.name user_loginuid=%user.loginuid %container.info
    shell=%proc.name parent=%proc.pname cmdline=%proc.cmdline terminal=%proc.tty container_id=%container.id image=%container.image.repository)
  priority: NOTICE
  tags: [container, shell, mitre_execution]

service falco restart

k exec -it pod -- sh

cat /var/log/syslog | grep falco | grep shell

=====

note: I use journalctl ...

user_loginuid=-1 nginx (id=217fe38f007f) shell=sh parent=runc cmdline=sh terminal=34816 container_id=217fe38f007f image=docker.io/library/nginx)
Mar 14 21:37:03 controlplane falco[29940]: 21:37:03.541857781: Notice A shell was spawned in a container with an attached terminal (user_id=0 repo=docker.io/library/nginx user=kc-internal user_loginuid=-1 nginx (id=217fe38f007f) shell=sh parent=runc cmdline=sh terminal=34816 container_id=217fe38f007f image=docker.io/library/nginx)
